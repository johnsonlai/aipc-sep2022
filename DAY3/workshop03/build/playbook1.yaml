---
- name: Configure image
  hosts: default
  become: true

  tasks:
  - name: Install Nginx
    apt:
      update_cache: true
      name: nginx
      state: latest
    register: output
    until: output.stderr == ""
  - name: Start nginx
    service:
      name: nginx
      enabled: true
      state: started